plugins {
    id "com.android.application"
}
//in config.gradle
configAndroidDomain project
configBaseDependences project
configLibDependences project
dependencies {
    implementation project(':Themvp')
}