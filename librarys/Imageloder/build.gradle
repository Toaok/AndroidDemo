apply plugin: 'com.android.library'

configAndroidDomain project
configModuleBaseDependences project
dependencies{
    implementation depLibConfig.glide
    annotationProcessor depLibConfig.glide_compiler
    implementation depLibConfig.glide_okhttp3_integration
}
